{
  "name": "repack-vue2",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open --env-mode development",
    "build": "rsbuild build",
    "preview": "rsbuild preview"
  },
  "dependencies": {
    "vue": "^2.7.14"
  },
  "engines": {
  "node": ">=16"
   },
  "devDependencies": {
    "@rsbuild/core": "^0.6.5",
    "@rsbuild/plugin-vue2": "^0.6.5"
  }
}
